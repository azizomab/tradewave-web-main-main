{% extends 'base.html.twig' %}

{% block title %}Publicite{% endblock %}

{% block body %}
    <h1>Publicite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publicite.id }}</td>
            </tr>
            <tr>
                <th>TypePub</th>
                <td>{{ publicite.typePub }}</td>
            </tr>
            <tr>
                <th>DatePub</th>
                <td>{{ publicite.datePub ? publicite.datePub|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ publicite.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_publicite_index') }}">back to list</a>

    <a href="{{ path('app_publicite_edit', {'id': publicite.id}) }}">edit</a>

    {{ include('publicite/_delete_form.html.twig') }}
{% endblock %}
